# Permissions

Check status:

```bash
sp computer-use permissions --json
```

Required permissions:

- Accessibility: inspect UI elements and perform accessibility actions.
- Screen Recording: capture app windows and screenshots.

If either permission is `missing`, ask the user to grant it in Supaterm Settings > Computer Use, then retry the command.
